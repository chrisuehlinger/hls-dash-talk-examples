{
  "name": "streaming-server-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:static": "http-server --port 8712 -c-1 -o",
    "start:browsersync": "cd public  && browser-sync start --server --port 8712 --files=\"./**/*\"",
    "start": "node index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-media-server": "^2.2.4"
  },
  "devDependencies": {
    "browser-sync": "^2.26.13",
    "http-server": "^0.12.3"
  }
}
